apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
spec:
  replicas: {{ .Values.replicas}}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app.image }}:{{ .Values.app.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
          - containerPort: {{ .Values.app.port }}
            protocol: TCP
        {{- if .Values.env }}
          env:
          {{- range $key,$val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
          {{- end }}  
<<<<<<< HEAD
        {{- end }}
=======
        {{- end }}
>>>>>>> 173d22260ad7ca86a1a9802c81c3c2b5f8cac2c3
